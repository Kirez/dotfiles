- defaults:
    link:
      relink: true
      create: false

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles: ''
    ~/.config/git: git
    ~/.config/zsh/.zshrc.local: zsh/zshrc.local
    ~/.zshenv: 
    ~/.vimrc: vim/vimrc
    ~/.vim: vim
