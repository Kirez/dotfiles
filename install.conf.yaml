- defaults:
    link:
      relink: true
      create: true

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles: ''
    ~/.config/git: git
    ~/.config/zsh/.zshrc.local: sh/zsh/zshrc.local
    ~/.zshenv: 
